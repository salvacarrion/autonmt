"""Text-processing pipeline: ``preprocessing`` (subword-agnostic filter/normalize/
dedupe), ``encoding`` (subword-dependent pretokenize/encode/decode), and the
low-level Moses/SPM ``tokenizers`` engines they sit on.
"""
