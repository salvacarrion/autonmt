"""Corpus inspection helpers: descriptive ``stats`` (token-length / vocabulary
summaries) and train<->test ``leakage`` detection. These *look at* a corpus and
report; they don't transform it (that's :mod:`autonmt.datasets.processing`).
"""
