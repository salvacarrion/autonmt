# Runtime dependencies. Source of truth lives in pyproject.toml; this file
# mirrors [project.dependencies] for the conda/pip workflows that still
# expect a flat requirements.txt.
#
# Optional extras live under [project.optional-dependencies] in pyproject.toml.
# Install them with:
#     pip install -e '.[hf]'        # HuggingFace dataset loader
#     pip install -e '.[wandb]'     # wandb logger
#     pip install -e '.[comet]'     # comet_ml logger
#     pip install -e '.[fairseq]'   # FairseqTranslator (deprecated; archived 2026-03-20)
#     pip install -e '.[dev]'       # pytest + flake8
#     pip install -e '.[all]'       # everything above

# Deep learning
torch
pytorch-lightning

# Numeric / data
numpy
pandas

# Plotting
matplotlib
seaborn

# Metrics
sacrebleu  # bleu, chrf, ter
bert-score

# NLP / tokenization
sacremoses
sentencepiece
tokenizers

# UX
tqdm

# Logging backend (used by pytorch-lightning's TensorBoardLogger)
tensorboard
